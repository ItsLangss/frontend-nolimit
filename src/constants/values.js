const values = {
  fontFamilyRoboto: [ 'Roboto', 'monospace', 'sans-serif' ].join(','),
  // DATA GRID
  dataGrid: {
    initialPaginationModel: {
      page: 0,
      pageSize: 10
    }
  }
}

export default values